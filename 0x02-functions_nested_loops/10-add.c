#include "holberton.h"
#include <stdio.h>

/**
 * add -  adds two integers and returns the result.
 * @a: value of number a
 * @b: value of number a
 * Return: void
 */
int add(int a, int b)
{
int sum;

sum = a + b;
return (sum);
}
