#include <stdio.h>

/**
 *  main - Entry point
 *
 *  Return: Always 0 (Sucess)
 */
int main(void)
{
	int letter = 122;

	while (letter >= 97)
	{

	putchar(letter);
	letter--;
}
putchar('\n');
return (0);
}
