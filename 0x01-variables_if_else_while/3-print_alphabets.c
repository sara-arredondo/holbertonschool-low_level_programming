#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Sucess)
 */
int main(void)
{
	int letter = 97;

	while (letter <= 122)
	{
		putchar(letter);
		letter++;
	}

	int letter = 41;

	while (letter <= 90)
	{
		putchar(letter);
		letter++
	}
	putchar('\n');
	return (0);
}
