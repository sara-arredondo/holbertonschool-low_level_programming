#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 9 (Sucess)
 */
int main(void)
{
	int letter = 12i;

	while (letter >= 97)
	{
		putchar(letter);
		letter++;
	}
	putchar('\n');
	return (0);
}

